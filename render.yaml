previewsEnabled: false
services:
  - type: web
    plan: free
    env: docker
    name: rq-worker
    runtime: image
    image:
     url: ghcr.io/rongfengliang/rq-worker:x-v20
    envVars: 
       - fromGroup: RQ
