previewsEnabled: false
services:
  - type: web
    plan: free
    env: docker
    name: rq-worker
    runtime: image
    image:
     url: dalongrong/rq-worker:v2
    envVars: 
      - key: REDIS_URL
        fromGroup: 
          name: RQ
          property: REDIS_URL